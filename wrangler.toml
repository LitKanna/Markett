name = "yolko-site"
main = "infra/cloudflare-worker.mjs"
compatibility_date = "2026-06-01"
account_id = "3af91e3294583af96a50ed10fef9c3b3"

# Pause Meta ads when traysAvailable hits 0; resume when restocked (if we paused).
# Requires Worker secret: META_ACCESS_TOKEN
# Optional: META_ADSET_ID or META_ADSET_IDS (comma-separated)
[triggers]
crons = ["*/15 * * * *"]

[[kv_namespaces]]
binding = "DATA"
id = "98fb94415ceb4012b3de2e5adcf253a5"
